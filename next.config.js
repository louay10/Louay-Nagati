/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    // Seulement les options valides pour Next.js 14
    // serverComponentsExternalPackages: [],
  },
}

module.exports = nextConfig